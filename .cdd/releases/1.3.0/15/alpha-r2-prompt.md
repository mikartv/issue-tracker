# α Dispatch — Cycle 15, Round 2

You are α for CDD cycle 15, round 2.

Read `.cdd/unreleased/15/beta-review.md`. Fix finding F-1. Push the fix commit on `cycle/15`.

Git identity before committing:

```bash
git config user.name "Alpha"
git config user.email "alpha@issue-tracker.cdd.cnos"
```
